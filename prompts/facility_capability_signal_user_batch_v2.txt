Review facility "$facility_name" (unique_id "$unique_id") for these $capability_count
capabilities: $capabilities

You are only setting sub-signals, not the final trust tier.

Return exactly one JSON object with this schema:
{
  "results": [
    {
      "capability": "icu",
      "claim_hit": true,
      "prose_hit": false,
      "screening_only": false,
      "capability_scope": "full_capability",
      "supporting_snippets": [],
      "confidence": 0.94,
      "reasoning": "Short audit-friendly explanation."
    }
  ]
}

Rules:
- Return exactly one result object for each requested capability. Do not omit any requested capability.
- `capability` must exactly match one requested capability from this request.
- `claim_hit`: true only if the facility explicitly claims this capability or a direct equivalent.
- `prose_hit`: true if the free-text description or procedure text materially supports the capability.
- `screening_only`: true only when the evidence is limited to screening, diagnostics, consultation,
  or awareness rather than the full capability.
- `capability_scope`: choose one short label:
  - `full_capability`
  - `adjacent_service`
  - `screening_or_diagnostics_only`
  - `unclear`
- `supporting_snippets`: up to 2 very short snippets copied only from the provided evidence.
- `confidence`: a number from 0 to 1.
- `reasoning`: one short explanation per capability, using only the provided evidence.

Evidence payload:
$llm_input_json
